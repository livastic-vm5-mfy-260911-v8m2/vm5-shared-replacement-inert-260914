name: vm5-capacity-hold

on:
  pull_request:

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Keep speculative check pending
        run: sleep 180
