name: vm5-capacity-hold

on:
  pull_request:

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Keep speculative check pending then fail closed
        run: |
          sleep 600
          exit 1
